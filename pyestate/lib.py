import numpy as np


def fixed_rate_amortization(principal, interest, n_payments):
	"""
	Returns an NX2 matrix where the Nth row has the principal and interest
	payments of a fixed rate loan.
	"""
	import pdb;pdb.set_trace()
